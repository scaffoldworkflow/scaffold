.variables:
  architectures:
    - amd64
    # - arm64
  docker_org: scaffoldworkflow
  oses:
    # - darwin
    # - freebsd
    - linux
    # - openbsd
    # - windows
  platforms:
    - linux/amd64
    - linux/arm64
    - linux/arm/v7
set-version:
  help: Update version constants from VERSION file
  cmd: |
    version = ?(cat VERSION).strip()
    sed -i 's/const VERSION = ".*"/const VERSION = "{version}"/' src/manager/constants/constants.go
build-docker-helpers:
  help: Build Helper Scaffold Docker image
  cmd: |
    stud set-version
    docker build -t scaffold:build -f helpers/Dockerfile.build .
    docker build -t scaffold:run --no-cache -f helpers/Dockerfile.run .
build-docker:
  help: Build Scaffold Docker image
  cmd: |
    stud set-version
    docker build -t scaffold-manager --no-cache -f src/manager/Dockerfile .
    docker build -t scaffold-worker --no-cache -f src/worker/Dockerfile .
build-docker-worker:
  help: Build Scaffold worker Docker image
  cmd: |
    stud set-version
    docker build -t scaffold-worker --no-cache -f src/worker/Dockerfile .
build-docker-manager:
  help: Build Scaffold manager Docker image
  cmd: |
    stud set-version
    docker build -t scaffold-manager --no-cache -f src/manager/Dockerfile .
push-docker:
  help: Build Scaffold Docker image
  cmd: |
    version = ?(cat VERSION).strip()
    docker tag scaffold-manager {docker_org}/scaffold-manager:{version}
    docker push {docker_org}/scaffold-manager:{version}
    docker tag scaffold-worker {docker_org}/scaffold-worker:latest
    docker push {docker_org}/scaffold-worker:latest
build-docker-test:
  help: Build Scaffold test Docker image
  cmd: |
    stud set-version
    docker build -t scaffold:test -f src/manager/test.Dockerfile .
publish:
  help: Publish Scaffold image
  cmd: |
    stud set-version
    platform_string = ','.join(platforms)
    version = ?(cat VERSION).strip()
    print(f"Running build command: docker buildx build --platform {platform_string} -t {docker_org}/scaffold:{version} -f src/manager/Dockerfile --push .")
    docker buildx build --platform {platform_string} -t {docker_org}/scaffold-manager:{version} -f src/manager/Dockerfile --push .
    docker buildx build --platform {platform_string} -t {docker_org}/scaffold-manager:latest -f src/manager/Dockerfile --push .
    print(f"Running build command: docker buildx build --platform {platform_string} -t {docker_org}/scaffold:{version} -f src/worker/Dockerfile --push .")
    docker buildx build --platform {platform_string} -t {docker_org}/scaffold-worker:{version} -f src/worker/Dockerfile --push .
    docker buildx build --platform {platform_string} -t {docker_org}/scaffold-worker:latest -f src/worker/Dockerfile --push .
    print('Done!')
build-cli:
  help: Build Scaffold CLI
  cmd: |
    # Clean up environment
    stud clean
    mkdir -p dist
    stud set-version
    # Enter source directory for CLI
    cd src/client
    # Build CLI for below oses and architectures and drop the binary into dist directories
    for arch_name in architectures:
      for os_name in oses:
        os_name_upper = os_name.upper()
        arch_name_upper = arch_name.upper()

        header_string = f"** BUILDING FOR {os_name_upper}/{arch_name_upper} **"
        header_width = len(header_string)
        header_line = "*" * header_width

        echo "{header_line}"
        echo "{header_string}"
        echo "{header_line}"

        output_path = f"../../dist/{os_name}/{arch_name}"
        mkdir -p "{output_path}"
        env GOOS="{os_name}" GOARCH="{arch_name}" CGO_ENABLED=0 go build -v -o scaffold
        mv scaffold "{output_path}"
    
    # Pop back up into root directory
    cd ../..
clean:
  help: Remove build and test artifacts
  cmd: |
    rm -r dist || true
kill-docker:
  help: Kill docker-compose deployments
  cmd: |
    docker-compose down --remove-orphans
    docker compose rm -f
run-docker:
  help: Deploy local images of Scaffold via docker compose
  cmd: |
    docker compose down --remove-orphans
    docker compose rm -f
    docker compose up
run-docker-test-http:
  help: Deploy local images of Scaffold via docker compose
  cmd: |
    docker compose down --remove-orphans
    docker compose rm -f
    docker compose -f test.http.docker-compose.yaml up
run-docker-test-https:
  help: Deploy local images of Scaffold via docker compose
  cmd: |
    docker compose down --remove-orphans
    docker compose rm -f
    docker compose -f test.https.docker-compose.yaml up 
run-test-http:
  help: Deploy local images of Scaffold via docker compose
  cmd: |
    ./test/run.sh 'http'
run-test-https:
  help: Deploy local images of Scaffold via docker compose
  cmd: |
    ./test/run.sh 'https'
run-test-both:
  help: Deploy local images of Scaffold via docker compose
  cmd: |
    ./test/run.sh 'both'
gen-certs:
  help: Generate local certificates for https testing
  cmd: |
    rm -rf temp
    mkdir -p temp

    echo "Generating root CA"
    $(openssl genrsa -out temp/rootCA.key 4096)
    $(openssl req -x509 -new -nodes -key temp/rootCA.key -subj "/C=US/ST=IL/O=Scaffold/CN=scaffold" -sha256 -days 1024 -out temp/rootCA.crt)
    echo "Done!"
    for idx, node_type in enumerate(['manager', 'worker']):
      echo "Generating {node_type} certs..."
      $(openssl genrsa -out temp/cert.{node_type}.key 2048)
      $(openssl req -new -sha256 -key temp/cert.{node_type}.key -subj "/C=US/ST=IL/O=Scaffold/CN=scaffold-{node_type}" -out temp/cert.{node_type}.csr)
      $(openssl x509 -req -extfile <(printf "subjectAltName=DNS:scaffold-{node_type},DNS:localhost") -in temp/cert.{node_type}.csr -CA temp/rootCA.crt -CAkey temp/rootCA.key -CAcreateserial -out temp/cert.{node_type}.crt -days 500 -sha256)
      echo "Done!"
      for cert_type in ['crt', 'key']:
        key_name = f'SCAFFOLD_TLS_CERT_{cert_type.upper()}'
        encoded = ?(cat temp/cert.{node_type}.{cert_type} | base64 -w 0)
        sed_idx = idx + 1
        sed -i 's/{key_name}:.* # {node_type}/{key_name}: "{encoded}" # {node_type}/' test.https.docker-compose.yaml
        echo "{node_type} cert.{cert_type} set"
docs:
  help: Generate Scaffold documentation
  cmd: |
    cd src/manager
    swag init
    cd ../../docs
    make html
